[project]
name = "adapter"
version = "0.1.0"
description = "Adapter for Sovereign Chess, variant by Mark Bates, engine by SeeStarz"
readme = "README.md"
authors = [{ name = "SeeStarz", email = "mfahrim7@gmail.com" }]
requires-python = ">=3.12"
dependencies = ["maturin>=1.15.0"]

[build-system]
requires = ["maturin>=1.15.0,<2"]
build-backend = "maturin"
